<div style="display:flex; align-items:center; gap:14px; margin-block-start:18px;">
    <img src="{{ $qrDataUri }}" alt="{{ __('documents.scan_to_verify') }}" style="width:120px; height:120px;">
    <div style="font-size:0.78rem; color:var(--doc-soft);">
        <div>{{ __('documents.scan_to_verify') }} / Scan to verify</div>
        <div style="margin-block-start:4px;">{{ __('documents.zatca_note') }}</div>
    </div>
</div>
